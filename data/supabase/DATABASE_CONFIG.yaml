database:
  engine: supabase
  type: postgresql_managed

account:
  provider: "supabase.com"
  owner_email: "<tu-email>"
  project_name: "omega-gym"
  project_ref: "jaltwjcipyrnmvjkdqdp"
  region: "us-east-1"
  environment: "development"

mcp:
  enabled: true
  mcp_server: "@supabase/mcp-server-supabase"

model_status:
  overall: active
  last_reviewed: 2026-08-03
  approved_by: "admin"
  notes: "Schema aplicado y en producción. Migración 009: cleanup — Drop tabla workout_logs (sin uso), Drop columna is_template de training_plans, borrado de plan de prueba 'si' y tips inactivos 'Anual'/'Trimestral'. Migración 010: catálogo de ejercicios completo — 1,324 filas en exercises (seed 009), media en public/exercises/ (~131 MB). Migración 010 (seed 010): 5 planes famosos (Push/Pull/Legs, Upper/Lower, Full Body, Bro Split, StrongLifts 5x5) con 90 ejercicios enlazados al catálogo; planes de prueba borrados. Migración 011 (seed 011): 3 rutinas de mujer (Strong Curves, Glute Focus, Thinner Leaner Stronger) con 58 ejercicios enlazados. Total: 8 planes, 148 ejercicios, 0 asignaciones."

tables:
  - name: profiles
    status: active
    rls_enabled: true
  - name: membership_types
    status: active
    rls_enabled: true
  - name: memberships
    status: active
    rls_enabled: true
  - name: payments
    status: active
    rls_enabled: true
  - name: training_plans
    status: active
    rls_enabled: true
  - name: plan_exercises
    status: active
    rls_enabled: true
  - name: plan_assignments
    status: active
    rls_enabled: true
  - name: check_ins
    status: active
    rls_enabled: true
  - name: exercises
    status: active
    rls_enabled: true

env_variables:
  - VITE_SUPABASE_URL
  - VITE_SUPABASE_ANON_KEY
